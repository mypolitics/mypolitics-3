import styled from "styled-components";
import Image from 'next/image';

export const AdditionalContentImage = styled(Image)`
  width: 100%;
  border-radius: 1rem;
  height: auto;
`;

export const ImageBox = styled.div`
  filter: drop-shadow(0px 0px 32px rgba(28, 87, 102, 0.2));
  width: 100%;
  border-radius: 1rem;
  height: auto;
`;
